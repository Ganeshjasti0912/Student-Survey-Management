apiVersion: v1
kind: Service
metadata:
  name: student-survey-frontend-svc
spec:
  type: NodePort
  selector:
    app: student-survey-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
