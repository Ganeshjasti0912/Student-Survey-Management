apiVersion: v1
kind: Service
metadata:
  name: student-survey-backend-svc
spec:
  type: NodePort
  selector:
    app: student-survey-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
